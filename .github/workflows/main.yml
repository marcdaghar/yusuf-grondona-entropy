name: Python syntax check

on: [push, pull_request]

jobs:
  syntax-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check Python syntax
        run: |
          set -e
          echo "Checking syntax of Python files..."
          # Utilise find au lieu de *.py pour éviter les problèmes de fichier non régulier
          find . -name "*.py" -type f -print0 | while IFS= read -r -d '' file; do
            python -m py_compile "$file"
            echo "✅ $file: valid syntax"
          done
find . -name "*.py" -type f -not -path "./currency_aesthetics/*" -print0 | while IFS= read -r -d '' file; do
